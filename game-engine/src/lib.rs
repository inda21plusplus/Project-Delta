pub mod renderer;
pub mod physics;
pub mod context;
pub use context::Context;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
